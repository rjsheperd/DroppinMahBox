<!-- Layout for individual file entries in a list -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">
    
    <!-- Icon shown next to the filename/subtitle -->
    <ImageView
        android:id="@+id/file_entry_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:padding="3dp" />
        
    <!-- Title of the file entry -->
    <TextView
        android:id="@+id/file_entry_title"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/file_entry_icon"
        android:layout_alignTop="@id/file_entry_icon"
	android:layout_margin="5dp"
        android:textSize="14sp"
        android:textStyle="bold" />
    
    <!-- Subtitle contains author and date -->
    <TextView
        android:id="@+id/file_entry_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/file_entry_title"
        android:layout_below="@id/file_entry_title" 
        android:textSize="12sp" />

</RelativeLayout>